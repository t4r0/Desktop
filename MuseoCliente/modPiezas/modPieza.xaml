<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	x:Class="MuseoCliente.modPieza"
	x:Name="UserControl"
	d:DesignWidth="640" d:DesignHeight="760">

    <DockPanel x:Name="LayoutRoot" Background="{DynamicResource White}" Loaded="LayoutRoot_Loaded">
        <Border DockPanel.Dock="Top" Margin="0" Height="30" VerticalAlignment="Top" BorderBrush="{DynamicResource Gray}" BorderThickness="0,0,0,2">
            <DockPanel>
                <Label x:Name="lblOperaciones" DockPanel.Dock="Left">[Op] Pieza</Label>
                <Label Height="30" VerticalContentAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0" Style="{DynamicResource CustomLabel}" DockPanel.Dock="Right">
                    <Button x:Name="btnCancelar" Height="27" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold">&#10060;</Button>
                </Label>
                <Label Height="30" VerticalContentAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0" Style="{DynamicResource CustomLabel}" DockPanel.Dock="Right">
                    <Button x:Name="btnGuardar" Height="27" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold">&#128190;</Button>
                </Label>
            </DockPanel>
        </Border>
        <Expander Header="Expander" Height="612" VerticalAlignment="Top" Style="{DynamicResource CustomExpander}" DockPanel.Dock="Top">
            <Grid Background="#FFE5E5E5"/>
        </Expander>
        <Expander Header="Expander" Height="730" VerticalAlignment="Top" Style="{DynamicResource CustomExpander}" DockPanel.Dock="Top">
            <Grid Background="#FFE5E5E5"/>
        </Expander>
        <Expander Header="Expander" Height="730" VerticalAlignment="Top" Style="{DynamicResource CustomExpander}" DockPanel.Dock="Top">
            <Grid Background="#FFE5E5E5"/>
        </Expander>
    </DockPanel>
</UserControl>